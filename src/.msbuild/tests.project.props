<?xml version="1.0" encoding="utf-8"?>

<Project>
  <PropertyGroup>

    <!-- Set IsTestProject to true if the project file name ends with "Tests" -->
    <IsTestProject>$(MSBuildProjectName.EndsWith(".Tests"))</IsTestProject>

  </PropertyGroup>
</Project>